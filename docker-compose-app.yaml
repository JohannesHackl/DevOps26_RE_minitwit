services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile-app
      args:
        GO_VERSION: "1.25.0"
    environment:
      DB_ADDR: ${DB_ADDR:-localhost}
    ports:
      - "5001:5001"

