services:
  web:
    build:
      context: .
      dockerfile: Dockerfile-app
    image: ${DOCKER_IMAGE:-kuba402/minitwit:latest}
    environment:
      DB_ADDR: ${DB_ADDR:-localhost}
    ports:
      - "5001:5001"
